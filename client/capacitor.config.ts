import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evinarija.app',
  appName: 'eVinarija',
  webDir: 'dist/client/browser'
};

export default config;
